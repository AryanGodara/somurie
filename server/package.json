{
  "name": "somurie-server",
  "version": "0.1.0",
  "description": "Hono-based API server for Farcaster metrics",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npx tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "hono": "^4.1.0",
    "@hono/node-server": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.2"
  }
}
